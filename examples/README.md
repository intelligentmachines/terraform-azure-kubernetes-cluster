### Example: How to create Kubernetes cluster in Azure using terraform.


### Instruction
* Replace the values in `terraform.tfvars` file
* Run `terraform init`
* Run `terraform plan`
* Run `terraform apply`